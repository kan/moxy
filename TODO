- Moxy::Plugin
 - store UI

- Store::SQLite
- plugin should be server implementation independent

- 内部コードが euc-jp なのがきもい
- 絵文字入力できるようにしたい
- Plugin::* は HTTP::Proxy に依存しないように。

- plugins
 - live http headers
   - by comet?
 - GPS Support(select point by google map?)
 - support utn
 - check contents size(max pdu)

- plugins: use render_template
- plugins: $self is not a self... just a $class.

- add tests.

http://kokogiko.net/m/archives/001955.html
